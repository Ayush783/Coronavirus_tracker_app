name: coronavirus_tracker_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^0.6.9
  auto_route_generator: ^0.6.10
  
  dartz: ^0.9.2
  flushbar: ^1.10.4
  flutter_bloc: ^6.1.1
  flutter_svg: ^0.18.0
  freezed_annotation: ^0.12.0
  get_it: ^5.0.1
  http: ^0.12.2
  injectable: ^1.0.5
  json_annotation: ^3.1.0
  kt_dart: ^0.8.0
  lottie: ^0.7.0+1
  meta: ^1.2.3

dev_dependencies:
  freezed: ^0.12.2
  build_runner: ^1.10.4
  injectable_generator: ^1.0.6
  json_serializable: ^3.5.0
  lint: ^1.3.0

flutter:
  uses-material-design: true
  assets:
    - images/

  fonts:
    - family: primary
      fonts:
        - asset: fonts/primary.ttf
